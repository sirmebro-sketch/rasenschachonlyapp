// Echte Avatar-Komponente als Bildbogen; Aufruf vom Repository-Stamm.
// node tools/portraet-bogen.cjs <Suffix> <Kopfform 0-13> [frisur|bart] [Haarfarbe 0-12]
const fs=require('fs'),{build}=require('esbuild'),sharp=(()=>{try{return require('sharp')}catch{if(process.env.CODEX_PRIMARY_RUNTIME_NODE_MODULES)return require(process.env.CODEX_PRIMARY_RUNTIME_NODE_MODULES+'/sharp');throw Error('Für Bildbögen wird sharp benötigt.');}})();
(async()=>{fs.mkdirSync('.preview',{recursive:true});const src=fs.readFileSync('App.jsx','utf8'),merkmal=process.argv[4]==='bart'?'bart':'frisur';const r=await build({stdin:{contents:src+`\nimport {renderToStaticMarkup} from 'react-dom/server'; export const portraits=(g)=>Array.from({length:${merkmal==='bart'?16:31}},(_,i)=>renderToStaticMarkup(<Avatar size={140} g={g} meta={{mk_haar:true}} zuege={{...zuegeAusKennung(1,g,'GER',{mk_haar:true}),stil:2,kopf:${Number(process.argv[3]||0)},haut:1,haar:${Number(process.argv[5]||1)},frisur:${merkmal==='frisur'?'i':1},bart:${merkmal==='bart'?'i':0},schmuck:0}}/>));`,resolveDir:process.cwd(),loader:'jsx'},bundle:true,format:'cjs',platform:'node',write:false});const m={exports:{}};new Function('module','exports','require',r.outputFiles[0].text)(m,m.exports,require);for(const g of merkmal==='bart'?['m']:['m','w']){const a=m.exports.portraits(g),over=[];for(let i=0;i<a.length;i++){over.push({input:await sharp(Buffer.from(a[i])).png().toBuffer(),left:(i%5)*170+15,top:Math.floor(i/5)*175+5});over.push({input:Buffer.from(`<svg width="160" height="22"><text x="15" y="17" fill="white" font-size="14">${g} · ${i}</text></svg>`),left:(i%5)*170,top:Math.floor(i/5)*175+146});}await sharp({create:{width:850,height:Math.ceil(a.length/5)*175,channels:4,background:'#23251e'}}).composite(over).png().toFile('.preview/'+g+'-'+(process.argv[2]||'before')+'.png');}})();
